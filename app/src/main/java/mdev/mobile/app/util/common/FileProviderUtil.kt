package mdev.mobile.app.util.common

import mdev.mobile.app.BuildConfig


const val FILE_PROVIDER_AUTHORITY = BuildConfig.APPLICATION_ID
// This path is the same as in the res/xml/log_paths.xml file
const val FILE_PATH_LOG_PATH = "logs/"
